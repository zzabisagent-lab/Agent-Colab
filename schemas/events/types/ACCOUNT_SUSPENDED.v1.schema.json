{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/ACCOUNT_SUSPENDED.v1.schema.json",
  "title": "ACCOUNT_SUSPENDED payload v1",
  "description": "Non-sensitive payload of ACCOUNT_SUSPENDED (account aggregate, Phase 1). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "account_id": {
      "type": "string",
      "minLength": 1
    },
    "reason_code": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "account_id",
    "reason_code"
  ],
  "additionalProperties": true
}
