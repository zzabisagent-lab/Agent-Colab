{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/APPROVAL_REJECTED.v1.schema.json",
  "title": "APPROVAL_REJECTED payload v1",
  "description": "Non-sensitive payload of APPROVAL_REJECTED (approval aggregate, Phase 1). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "approval_id": {
      "type": "string",
      "minLength": 1
    },
    "decided_by": {
      "type": "string",
      "minLength": 1
    },
    "reason_code": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "approval_id",
    "decided_by",
    "reason_code"
  ],
  "additionalProperties": true
}
