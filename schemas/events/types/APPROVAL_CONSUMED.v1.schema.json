{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/APPROVAL_CONSUMED.v1.schema.json",
  "title": "APPROVAL_CONSUMED payload v1",
  "description": "Non-sensitive payload of APPROVAL_CONSUMED (approval aggregate, Phase 1). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "approval_id": {
      "type": "string",
      "minLength": 1
    },
    "consumption_key": {
      "type": "string",
      "minLength": 1
    },
    "used_count": {
      "type": "integer",
      "minimum": 0
    }
  },
  "required": [
    "approval_id",
    "consumption_key",
    "used_count"
  ],
  "additionalProperties": true
}
