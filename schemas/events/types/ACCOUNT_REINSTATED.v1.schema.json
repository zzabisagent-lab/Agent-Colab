{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/ACCOUNT_REINSTATED.v1.schema.json",
  "title": "ACCOUNT_REINSTATED payload v1",
  "description": "Non-sensitive payload of ACCOUNT_REINSTATED (account aggregate, Phase 4). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "account_id": {
      "type": "string",
      "minLength": 1
    },
    "reason_code": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "account_id",
    "reason_code"
  ],
  "additionalProperties": true,
  "x-extension": "development plan §11.1: suspension lifted (P4-01)"
}
