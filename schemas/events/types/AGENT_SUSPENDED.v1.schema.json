{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/AGENT_SUSPENDED.v1.schema.json",
  "title": "AGENT_SUSPENDED payload v1",
  "description": "Non-sensitive payload of AGENT_SUSPENDED (agent aggregate, Phase 3). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "agent_id": {
      "type": "string",
      "minLength": 1
    },
    "reason_code": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "agent_id",
    "reason_code"
  ],
  "additionalProperties": true
}
