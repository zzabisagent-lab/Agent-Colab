{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/ACCOUNT_CREATED.v1.schema.json",
  "title": "ACCOUNT_CREATED payload v1",
  "description": "Non-sensitive payload of ACCOUNT_CREATED (account aggregate, Phase 1). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "account_id": {
      "type": "string",
      "minLength": 1
    },
    "account_type": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "account_id",
    "account_type"
  ],
  "additionalProperties": true,
  "x-extension": "spec §9.1 Account entity lifecycle"
}
