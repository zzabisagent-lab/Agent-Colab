{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/APPROVAL_EXPIRED.v1.schema.json",
  "title": "APPROVAL_EXPIRED payload v1",
  "description": "Non-sensitive payload of APPROVAL_EXPIRED (approval aggregate, Phase 1). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "approval_id": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "approval_id"
  ],
  "additionalProperties": true
}
