{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/SECRET_GRANT_REVOKED.v1.schema.json",
  "title": "SECRET_GRANT_REVOKED payload v1",
  "description": "Non-sensitive payload of SECRET_GRANT_REVOKED (secret_grant aggregate, Phase 4). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "grant_id": {
      "type": "string",
      "minLength": 1
    },
    "reason_code": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "grant_id",
    "reason_code"
  ],
  "additionalProperties": true
}
