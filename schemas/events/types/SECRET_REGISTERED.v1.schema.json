{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/SECRET_REGISTERED.v1.schema.json",
  "title": "SECRET_REGISTERED payload v1",
  "description": "Non-sensitive payload of SECRET_REGISTERED (secret aggregate, Phase 4). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "secret_id": {
      "type": "string",
      "minLength": 1
    },
    "provider": {
      "type": "string",
      "minLength": 1
    },
    "version": {
      "type": "integer",
      "minimum": 0
    }
  },
  "required": [
    "secret_id",
    "provider",
    "version"
  ],
  "additionalProperties": true
}
