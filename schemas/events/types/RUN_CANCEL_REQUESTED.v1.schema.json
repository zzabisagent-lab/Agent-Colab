{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/RUN_CANCEL_REQUESTED.v1.schema.json",
  "title": "RUN_CANCEL_REQUESTED payload v1",
  "description": "Non-sensitive payload of RUN_CANCEL_REQUESTED (schedule_run aggregate, Phase 5). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "run_id": {
      "type": "string",
      "minLength": 1
    },
    "reason_code": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "run_id",
    "reason_code"
  ],
  "additionalProperties": true
}
