{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/AGENT_UPDATED.v1.schema.json",
  "title": "AGENT_UPDATED payload v1",
  "description": "Non-sensitive payload of AGENT_UPDATED (agent aggregate, Phase 3). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "agent_id": {
      "type": "string",
      "minLength": 1
    },
    "changed_fields": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "agent_id",
    "changed_fields"
  ],
  "additionalProperties": true
}
