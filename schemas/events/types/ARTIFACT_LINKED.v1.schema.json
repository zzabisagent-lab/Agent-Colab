{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/ARTIFACT_LINKED.v1.schema.json",
  "title": "ARTIFACT_LINKED payload v1",
  "description": "Non-sensitive payload of ARTIFACT_LINKED (artifact aggregate, Phase 1). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "artifact_id": {
      "type": "string",
      "minLength": 1
    },
    "subject_type": {
      "type": "string",
      "minLength": 1
    },
    "subject_id": {
      "type": "string",
      "minLength": 1
    },
    "relation": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "artifact_id",
    "subject_type",
    "subject_id",
    "relation"
  ],
  "additionalProperties": true,
  "x-extension": "spec §9.1 ArtifactLink transitions leave an Event (P1-09)"
}
