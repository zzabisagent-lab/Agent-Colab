{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-colab.dev/schemas/events/types/APPROVAL_GRANTED.v1.schema.json",
  "title": "APPROVAL_GRANTED payload v1",
  "description": "Non-sensitive payload of APPROVAL_GRANTED (approval aggregate, Phase 1). Sensitive content goes to the encrypted envelope.",
  "type": "object",
  "properties": {
    "approval_id": {
      "type": "string",
      "minLength": 1
    },
    "decided_by": {
      "type": "string",
      "minLength": 1
    },
    "quorum_count": {
      "type": "integer",
      "minimum": 0
    }
  },
  "required": [
    "approval_id",
    "decided_by",
    "quorum_count"
  ],
  "additionalProperties": true
}
