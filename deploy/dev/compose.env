# Non-secret defaults for the development Compose stack. Pass with
# `docker compose --env-file deploy/dev/compose.env ...` (the repository's `.env` is the
# deployment-secrets file and is never read by Compose).
COMPOSE_PROJECT_NAME=agent-colab
POSTGRES_PASSWORD=colab-dev-only
